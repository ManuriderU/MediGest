<Window x:Class="MediGest.CrearUsuario"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediGest.Pages"
        mc:Ignorable="d"
        Title="Crear Usuario" Height="232" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Login -->
        <TextBlock Text="Login:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="txtLogin" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <!-- Contraseña -->
        <TextBlock Text="Contraseña:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="txtPassword" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Confirmacion:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Top" Margin="0,6,0,0"/>
        <PasswordBox x:Name="txtPasswordConfirmacion" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <!-- Rol -->
        <TextBlock Text="Rol:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="cmbRol" Grid.Row="3" Grid.Column="1" Margin="5">
            <ComboBoxItem Content="Rol" IsSelected="True" IsEnabled="False"/>
            <ComboBoxItem Content="Admin"/>
            <ComboBoxItem Content="Medico"/>
            <ComboBoxItem Content="Recepcionista"/>
        </ComboBox>
        
        <!-- Botones -->
        <StackPanel Grid.Row="9" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button x:Name="btnCancelar"
                    Content="Cancelar"
                    Width="90"
                    Margin="0,0,10,0"
                    Click="btnCancelar_Click"/>
            <Button x:Name="btnGuardar"
                    Content="Guardar"
                    Width="90"
                    Click="btnGuardar_Click"/>
        </StackPanel>
    </Grid>
</Window>