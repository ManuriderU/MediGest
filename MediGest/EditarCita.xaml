<Window x:Class="MediGest.EditarCita"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediGest.Pages"
        mc:Ignorable="d"
        Title="Editor de Cita" Height="330" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Fecha de la Cita -->
        <TextBlock Text="Fecha de la Cita:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <DatePicker x:Name="dpFechaCita"
            Grid.Row="0"
            Grid.Column="1"
            Margin="5"
            SelectedDateFormat="Short"/>

        <!-- Hora -->
        <TextBlock Text="Hora:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="cmbHora" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <!-- Estado -->
        <TextBlock Text="Estado:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="cmbEstado" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <!-- Observaciones -->
        <TextBlock Text="Observaciones:" Grid.Row="6" VerticalAlignment="Top" Margin="0,31,169,0" Grid.RowSpan="3" Grid.Column="1"/>
        <TextBox x:Name="txtObservaciones" Grid.Row="6" Margin="0,47,5,3" TextWrapping="Wrap" AcceptsReturn="True" Grid.ColumnSpan="2" Grid.RowSpan="3"/>

        <!-- Botones -->
        <StackPanel Grid.Row="9" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button x:Name="btnCancelar"
                    Content="Cancelar"
                    Width="90"
                    Margin="0,0,10,0"
                    Click="btnCancelar_Click"/>
            <Button x:Name="btnGuardar"
                    Content="Guardar"
                    Width="90"
                    Click="btnGuardar_Click"/>
        </StackPanel>
    </Grid>
</Window>