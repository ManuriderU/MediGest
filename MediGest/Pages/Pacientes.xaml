<Page x:Class="MediGest.Pages.Pacientes"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MediGest.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Pacientes">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Background="#F3F4F6">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Pacientes con Citas" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Foreground="#1A1A1A"/>
                    <TextBlock Text="Lista de pacientes que tienen cita programada" 
                           FontSize="14" 
                           Foreground="#6C757D"
                           Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Search Box -->
                <Border Grid.Row="1" 
                    Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Search Icon -->
                        <TextBlock Grid.Column="0" 
                               Text="🔍" 
                               FontSize="16" 
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"
                               Foreground="#6C757D"/>

                        <!-- Search TextBox -->
                        <TextBox Grid.Column="1" 
                             x:Name="SearchTextBox"
                             BorderThickness="0"
                             VerticalAlignment="Center"
                             FontSize="14"
                             TextChanged="SearchTextBox_TextChanged"
                             Tag="Buscar pacientes por nombre o apellido..."/>
                    </Grid>
                </Border>

                <!-- Patient List -->
                <Border Grid.Row="2">
                    <DataGrid x:Name="PatientsDataGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          >

                        <!-- Columns -->
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                            <DataGridTextColumn Header="Apellido" Binding="{Binding Apellido}" Width="*"/>
                            <DataGridTemplateColumn Header="⏰ Hora de Cita" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Cita}" 
                                               Foreground="#2563EB" 
                                               FontWeight="SemiBold"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
